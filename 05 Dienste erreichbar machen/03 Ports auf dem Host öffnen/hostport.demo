# Ports auf dem Host öffnen

## Cluster ausrollen
kind create cluster --config kind.yaml

## Anwendung mit hostPort anschauen und ausrollen
cat app.yaml
kubectl apply -f app.yaml

## Zugriff über hostPort testen
curl localhost:8080